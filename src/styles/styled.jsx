import styled from 'styled-components';

const BigPlane = styled.img`
  display: block;
  width: 60px;
`;

const GrayText = styled.span`
  font-size: 16px;
  color: #f1f1f1;
`;

export { BigPlane, GrayText };